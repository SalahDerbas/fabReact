import { CSSProperties } from "react"

const menuStyles1: CSSProperties = {
    marginTop: "-30px",
    marginLeft: "-51px",
}

const avatarStyles: CSSProperties = {
    border: "1px solid #000000",
    height: "43px",
    width: "43px",
    borderRadius: "50%",
    backgroundColor: "#ffffff",
    color: "#000000",
    marginRight: "12px",
}

const sidebarStyle: CSSProperties = {
    width: "15rem",
    // marginTop: "56px",
    zIndex: 1,
}

export { menuStyles1, sidebarStyle, avatarStyles }